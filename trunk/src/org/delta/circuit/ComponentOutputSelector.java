package org.delta.circuit;

public class ComponentOutputSelector extends OutputSelector<Component> {

    public ComponentOutputSelector(Component component, int outputNumber) {
        super(component, outputNumber);
    }

}
